# Local model evaluation — 2026-09-06

Both exercises reached acceptance using `qwen3.8-27b` at the user-provided `http://10.134.6.235:8080/v1`, without an API key. These are two functional demonstrations on one model, not a benchmark or a comparison of model sizes.

| Exercise | Fresh test outcomes | Completed model responses | Generated tokens | Largest message context |
| --- | --- | ---: | ---: | ---: |
| Greeting | reject, reject, pass | 5 | 552 | 6921 bytes |
| C build + README | reject, pass | 6 | 1152 | 12630 bytes |

The greeting's first model request returned HTTP 500. Sisyphus stopped as an infrastructure error, and explicit resume tested the unchanged recipe again before asking for another repair. The successful repair read the test and recipe, edited the greeting, checked it in the workshop, and requested a fresh test. One earlier baseline trial already had the right greeting and is excluded from these figures.

The C exercise started with subtraction instead of addition and no README. The model read the test/source/build script, corrected the C program, added user instructions, compiled and checked it locally, then requested a fresh host test. Three parametrized cases passed in separate clean containers. The accepted source includes the workshop-produced binary because source/generated separation is not implemented yet; the provided build script recompiles it during the tests. This does not claim a general clean-source rebuild guarantee.

The server reported 9710 and 19525 prompt tokens summed across completed requests respectively; these totals include repeated/cached prompt content, and are not unique context sizes. Counts exclude the failed HTTP request and a separate diagnostic request used to check the endpoint. Exact run IDs, pinned image IDs, request/tool counts, and usage totals are in EVALUATION.json.

The complete saved runs are archived locally under `.sisyphus/evaluations/2026-09-06/{hello,compile}/`, including transcripts, tool logs, trial evidence, accepted recipes, and acceptance manifests. These are evidence archives copied from temporary test projects; their recorded absolute paths refer to those original projects. Do not resume an archived copy. Start a new run from an accepted recipe to reproduce its behavior.

The greeting began with an existing README; the compiler README was generated by the model. Original model examples remain deliberately broken. The local endpoint, compiler image, and accepted binaries were not published.

Automated verification: 128 passing tests, including 18 real-container checks. Those checks use a scripted HTTP provider and run offline; they do not contact the live model. Lint and wheel/source builds passed. Research/download access, other providers/model sizes, kernel builds, hardware interaction, and clean-source rebuild proofs remain untested/unimplemented.
